Coded using Python 2.7

To run, ensure TensorFlow is installed. It can be installed with pip:
"pip install tensorflow"

To run: "python tensor.py"
To display plots, uncomment lines 31-35

Included in this directory is the original assignment prompt for this program.
The report.pdf document contains the answers to the assignment.
The only missing component is that instead of displaying a plot, I only reported the values of the weights.